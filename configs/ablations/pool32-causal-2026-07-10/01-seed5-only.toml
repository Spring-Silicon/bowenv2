extends = "../../bases/gated-pool32-legacy-screen.toml"

# The only change from the failed legacy/modern bridge is trainer seed 42 -> 5.
[trainer]
seed = 5

[wandb]
run_name = "pool32-causal-seed5-only-1"

[paths]
run_dir = "runs/pool32-causal-seed5-only-1"
