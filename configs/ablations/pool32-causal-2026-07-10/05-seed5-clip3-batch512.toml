extends = "../../bases/gated-pool32-legacy-screen.toml"

# Every seed-5 plus one-factor branch failed; test the AdamW combination that
# previously worked without the trajectory pool.
[trainer]
seed = 5
grad_clip = 3.0
batch = 512

[wandb]
run_name = "pool32-causal-seed5-clip3-batch512-1"

[paths]
run_dir = "runs/pool32-causal-seed5-clip3-batch512-1"
